# Handback — the identifiers rail

D1 built `rails/identifiers/index.ts` against the parser at `rails/diff.ts:31`.
The ruling is ADR 0003; the shape follows ADR 0005.
Nothing outside the rail moved, and upstream-org/project is untouched.
