# Handback — the fact-light rail

D1 built `rails/fact-light/index.ts` against the parser at `rails/diff.ts:900`.
The ruling is ADR 0009; the shape follows ADR 0003.
Nothing outside the rail moved, and upstream-org/project is untouched.
