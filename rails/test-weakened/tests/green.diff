diff --git a/src/tests/parse.test.ts b/src/tests/parse.test.ts
--- a/src/tests/parse.test.ts
+++ b/src/tests/parse.test.ts
@@ -4,8 +4,10 @@ import { parse } from '../parse.ts'
 
 test('parses a hunk header', () => {
   const out = parse(FIXTURE)
-  expect(out[0].line).toBeDefined()
+  expect(out[0].line).toBe(12)
+  expect(out[0].path).toBe('src/x.ts')
 })
