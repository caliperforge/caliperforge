package pay.ledger

fun receipt(name: String): String {
    val body = """
        { "name": "$name",
          "lines": [
            { "kind": "charge" }
          ]
        }
        }}} closes nothing
        {{{ opens nothing
        // starts no comment
        /* starts no comment
    """
    return body.trimIndent()
}
