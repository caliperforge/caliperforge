# median(xs)

Add `median(xs: number[]): number` to `src/stats.ts`. It returns the median of a non-empty list of
numbers — the middle value at odd length, the mean of the two middle values at even length. It must
not change the list the caller passed.
